# 服务端口号
server:
  port: 8080

# 数据库配置
spring:
  datasource: # 数据库配置
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springcloud?useSSL=false&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
    username: root
    password: lIIl0.*.lIIl
    hikari:
      maximum-pool-size: 10 # 最大连接池数
      max-lifetime: 1770000